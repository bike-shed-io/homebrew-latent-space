# Homebrew Tap for Latent Space

Install the macOS desktop app with:

```bash
brew tap bike-shed-io/latent-space
brew install --cask latent-space
```

The cask downloads the latest public desktop release from `bike-shed-io/latent-space-releases`.
